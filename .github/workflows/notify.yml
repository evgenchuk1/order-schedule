name: 📦 הזמנות — תזכורות

on:
  # Every 10 min, Sun–Thu + Sat (test), 05:00–16:00 UTC = 08:00–19:00 Israel (UTC+3 summer)
  schedule:
    - cron: '*/10 5-16 * * 0,1,2,3,4,6'
  workflow_dispatch: {}

jobs:
  notify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Send order push notifications
        run: python3 .github/scripts/notify.py
